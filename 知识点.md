# 核心知识点 · 「大道至简」博客

> 每完成清单中的一个任务，把该步的核心知识点记在这里。格式：`### 任务编号 标题` → 收获 / 踩坑 / 示例代码。
> 已经踩过的坑不要再踩：先查这里。

---

## M1 · 基建

### M1-1 Next.js 16（App Router）关键差异
- **版本现状**：`create-next-app@latest` 装到的是 Next.js 16.3 + React 19.2 + Turbopack，不是文档里常见的 15。
- **typed route props**：`page` / `layout` 直接用全局类型 `PageProps<'/route'>` / `LayoutProps<'/route'>`（`next dev/build` 时生成），无需手写类型。
- **`params` 是 Promise**：动态路由 `{ params }: { params: Promise<{ slug: string }> }`，必须 `await params`。
- **AGENTS.md**：脚手架自带，提示「这不是你熟悉的 Next.js」，改代码前应看 `node_modules/next/dist/docs/`。

### M1-2 Tailwind v4（CSS 配置化，无 tailwind.config.js）
- 入口 `@import "tailwindcss";`；配置全在 CSS 里用 `@theme`。
- 自定义颜色 → `@theme inline { --color-paper: rgb(var(--c-paper)); ... }`，自动生成 `bg-paper / text-ink / border-line` 等工具类。
- 类名暗黑模式：`@custom-variant dark (&:where(.dark, .dark *));`。
- `@theme inline` 会把值内联进工具类，所以自定义 CSS 里**不要引用 `var(--color-*)`**（可能未定义），直接写字面量。

### M1-3 Prisma 7 破坏性变更（与 readme 旧写法完全不同）
- **`url` 移出 schema** → 进 `prisma.config.ts`：
  ```ts
  export default defineConfig({
    schema: "prisma/schema.prisma",
    migrations: { path: "prisma/migrations", seed: "tsx prisma/seed.ts" },
    datasource: { url: env("DATABASE_URL") },
  });
  ```
- **client 生成方式**：generator 用 `provider = "prisma-client"`（不是 `prisma-client-js`）+ 必须指定 `output`；生成到 `lib/generated/prisma/client`。
- **必须 driver adapter**：
  ```ts
  import { PrismaBetterSqlite3 } from "@prisma/adapter-better-sqlite3";
  const prisma = new PrismaClient({ adapter: new PrismaBetterSqlite3({ url: "file:./dev.db" }) });
  ```
  ⚠️ 导出名是 `PrismaBetterSqlite3`（小写 q），不是 `PrismaBetterSQLite3`。
- **seed 配置**在 `prisma.config.ts` 的 `migrations.seed`（不在 package.json）。
- **SQLite 限制**：不支持 `@db.Text`（去掉即可，String 默认就是 TEXT）；不支持 `@@fulltext`（全文检索留到生产 PostgreSQL 用 `tsvector`）。`previewFeatures = ["fullTextSearch"]` 已废弃，删掉。

### M1-4 国内网络字体方案：@fontsource
- `next/font/google` 编译时会去 `fonts.googleapis.com` 下载 → 国内连不上会**卡死 dev/build**（日志：`Error while requesting resource`）。
- 改用 npm 包（安装即下载好）：
  ```ts
  import "@fontsource/noto-serif-sc/500.css"; // 按字重引入
  import "@fontsource/ibm-plex-mono/400.css";
  ```
  家族名：`"Noto Serif SC"` / `"Noto Sans SC"` / `"IBM Plex Mono"`，在 `@theme` 里映射 `--font-serif/sans/mono`。

### M1-5 设计系统暗黑模式：CSS 变量整体翻转
- token 用 `rgb(r g b)` 三段数字存 `:root` 与 `html.dark` 各一套；Tailwind 颜色写成 `rgb(var(--c-paper))` → `html.dark` 一加，所有 `bg-paper / text-ink` 自动翻转，**不需要 `dark:` 变体**。
- 防闪白：根布局 `<body>` 首元素同步脚本读 localStorage / `prefers-color-scheme`，首帧前加 `dark` class；`<html>` 加 `suppressHydrationWarning`。
- 反色带（通告条/代码块/订阅区）用 `bg-night text-nighttext`（恒定不翻转），不要用 `bg-ink`。

### M1-6 数据库切 PostgreSQL（Docker 本地）
- **docker-compose.yml**：`postgres:16-alpine` + 端口 `5432:5432` + 命名卷持久化 + `pg_isready` 健康检查；`docker compose up -d` 启动，数据在卷里保留。
- **Prisma 7 的 PG adapter**：
  ```ts
  import { PrismaPg } from "@prisma/adapter-pg";
  const prisma = new PrismaClient({ adapter: new PrismaPg({ connectionString: process.env.DATABASE_URL! }) });
  ```
  `lib/db.ts` 与 `prisma/seed.ts` 都要换成它（seed 走 tsx，别引用 `@/` 别名，用相对路径）。
- ⚠️ **Windows + Docker 大坑**：`localhost` 会解析到 `::1`(IPv6 loopback)，而 `::1:5432` 上是 `wslrelay.exe`（WSL 中继）而非 Postgres → 连接超时 `P1001`。**必须用 `127.0.0.1`**：
  `DATABASE_URL="postgresql://postgres:postgres@127.0.0.1:5432/mini_blog?schema=public"`
- **切换 DB 后必须重建**：删旧 `prisma/migrations/` + 旧库文件 → `prisma migrate dev --name init` → **`prisma generate`**（migrate dev 有时不刷新 client，provider 变了会报「adapter 与 provider sqlite 不兼容」）→ `prisma db seed`。
- Postgres 下 `String` 默认映射 `text`，原 `@db.Text` 无需加回；全文检索可在此后启用（生产推荐 `tsvector`）。

### 数据初始化（database init）完整流程
| 场景 | 命令 | 作用 |
|---|---|---|
| 首次开发 | `docker compose up -d` → `npx prisma migrate dev --name init` → `npx prisma db seed` | 建库 + 建表 + 造数据 |
| 改 schema 后 | `npx prisma migrate dev --name <描述>` | 生成并应用新迁移 |
| 推倒重来 | `npx prisma migrate reset` | 删表重建 + 自动跑 seed |
| 生产部署 | `npx prisma migrate deploy` + `npx prisma db seed` | 只应用迁移，不生成 |
- seed 可重复执行：`User/Tag/Post` 用 `upsert`（唯一键 email/slug），`Shiyu` 先 `deleteMany` 再重建。
- 默认管理员：**`admin / admin123`**（用户名 `admin`，邮箱 `1372553910@qq.com`；bcrypt 存哈希）。登录支持「账号或邮箱」。

---

## M2 · 内容模块（MDX + 文章链路线）

### M2-1 next-mdx-remote（v6）接入
- 服务端组件用 `compileMDX`（来自 `next-mdx-remote/rsc`）把 MDX 字符串编译为 React 元素：
  ```ts
  const { content } = await compileMDX({
    source,                                  // MDX 字符串
    components,                              // 自定义组件映射
    options: {
      parseFrontmatter: false,
      mdxOptions: { remarkPlugins: [remarkGfm], rehypePlugins: [rehypeSlug] },
    },
  });
  ```
- `remark-gfm`：表格 / 删除线等 GFM 语法；`rehype-slug`：给标题自动加 `id` 锚点。
- ⚠️ **`<邮箱@域名>` 会炸编译**：MDX 把 `<your@email.com>` 当 JSX 标签解析（`@` 非法）报 `Unexpected character '@' in name`；普通 Markdown 里它是邮箱自动链接。已在 `renderMDX` 里加防御预处理：`<xxx@yyy.tld>` → `[xxx](mailto:...)`。写文章时邮箱请用 `[x@y.com](mailto:x@y.com)` 或行内代码。
- **Mermaid 图表**：文章里 ` ```mermaid ` 代码块 → 自定义 remark 插件（`lib/mdx.ts` 的 `remarkMermaid`）把该 code 节点转成 `<Mermaid code="..." />`（mdxJsxFlowElement）→ `components/posts/Mermaid.tsx` 客户端用动态 import 懒加载 mermaid 渲染 SVG。**mermaid 是浏览器运行时，选客户端渲染**（build-time 渲染要装 Playwright/Chromium 太重）；懒加载保证只有含图页面才下载。支持时序图/流程图/类图/ER 等。
- **后台编辑器 Markdown 预览**：`PostForm` 加「编辑 / 预览」切换；预览用 `react-markdown` + `remark-gfm` 客户端渲染，复用前台 MDX 纸感组件（`PostPreview.tsx`），mermaid 代码块经 `pre` 组件覆盖交给 `<Mermaid>` 渲染。注意：react-markdown 会给组件注入 `node` 等内部 props，需先剥掉再传给纸感组件（否则泄漏到 DOM 出警告）。
- **架构决策**：文章内容以 **DB 为准**（`Post.content` 存 MDX 字符串），不再用 `content/posts/*.mdx` 文件——避免双数据源，且与后台 CRUD（M4）一致。

### M2-2 目录（TOC）提取：github-slugger 与 rehype-slug 对齐
- `rehype-slug` 内部用 `github-slugger` 生成 slug。自建 TOC 时也 `new GithubSlugger()` 逐个 `slugger.slug(text)`，并**把 h1..h6 全部喂给它**（保持计数状态一致），只把 h2/h3 记入 toc——否则重复标题 slug 会撞号。
- 用 `^#{1,6}\s+` 匹配标题行，并跳过 ` ``` ` 代码块内的。

### M2-3 SSG + 数据库（Next 16 惯例）
- 列表 / 详情直接用 **async server component** `await` Prisma 查询（不经 API）。
- 动态路由 SSG：`generateStaticParams()` 返回所有 slug → 构建期预渲染（`● (SSG)`）；`params` 是 **Promise**，必须 `await params`。
- `generateMetadata()` 里同样能 `await` 查库生成 title/description（SEO）。

### M2-4 自定义 MDX 组件（纸感排版）
- `code` 组件区分行内 / 代码块：MDX 给代码块的 `<code>` 加 `className="language-xx"`，据此返回不同样式——行内用纸底 + mono，代码块交给 `pre`（`bg-night text-nighttext`）。
- 首字下沉：`.mdx-body > p:first-of-type::first-letter`（float + 大号宋体 + 朱红）。
- 正文用宋体：在正文容器上 `font-serif` 一次设定，各元素继承。

### M2-5 标签 / 归档页
- 按月分组：`Map<"YYYY-MM", { posts }>` 一趟遍历；时间线用 `border-l` 竖线 + `absolute` 定位的圆点节点（`-left-[5px]` + `ring-4 ring-paper`）。
- 侧栏 `.tag-link.active` 左缘朱红条；移动端横向 `.filter-chip` 滚动条。
- 首页数据台账：`prisma.post.count` / `aggregate(_sum.views)` 等做真实计数。

---

## M3 · 拾语模块（动态流）

### M3-1 拾语查询封装 + `no` 序号
- Shiyu 表新增 `no Int @default(0)` 流水序号（展示 `N°036`），种子按 N°027–036 写入；新建取 `max(no)+1`（`getNextShiyuNo`）。排序 **置顶优先 + 时间倒序**：`orderBy: [{ pinned: "desc" }, { createdAt: "desc" }]`。
- 分页：`skip/take` + 与 `count` 用 `Promise.all` 并行；返回 `{ items, total, page, pageSize, hasMore }`。
- 配图 `images` 是 JSON 数组字符串（可空），查询层解析为 `string[] | null`、日期转 ISO——统一在 `toShiyuItem` 序列化，页面与 API 复用同一函数。

### M3-2 Route Handler（Next 16 惯例）
- 动态段 `params` 是 **Promise**：`{ params }: { params: Promise<{ id: string }> }`，必须 `await params`（与页面一致）。
- Route Handler 默认不缓存（GET 也不缓存），`GET` 里读 DB 即动态渲染（构建输出标 `ƒ`）；页面 ISR 用 `export const revalidate = 60`（旧模型，未启用 Cache Components）→ 构建输出 `○ 1m`。
- 写操作（POST/PUT/DELETE）统一 `requireAdmin()` 守卫：M3 阶段 `lib/auth.ts` 占位恒 401（返回 null），M4 接 `getServerSession` 后替换函数体即可，路由结构不变。
- 写后 `revalidatePath("/shiyu")` + `revalidatePath("/")`（首页台账含拾语计数），刷新 ISR 缓存。

### M3-3 组件复用边界：Server 页 + 客户端加载更多
- 一个 presentational 组件（`ShiyuTimeline`，渲染 `<li>`）被 `"use client"` 的 `ShiyuStream` 引用后，整个模块边界内都变客户端组件——但**初始 HTML 仍 SSR**（客户端组件也参与服务端渲染），数据由服务端页作为 props 传入，首屏不额外请求。
- 加载更多：客户端 `fetch('/api/shiyu?page=N')` 追加 `<li>`；时间线圆点配色按「全局序号」分档（前 5 朱红→中 3 金→余青灰），`startIndex` 传入保证翻页不跳色。
- `next/image` 懒加载：须在 `next.config.ts` 配 `images.remotePatterns`（种子用 `images.unsplash.com`）；`fill` + 容器 `aspect-[16/9]` + `sizes`。

### M3-4 验证踩坑
- 构建产物里 `/shiyu` 显示 `○ 1m 1y` = ISR 生效（1 分钟重验证）。
- **React SSR 数字子节点会被 `<!-- -->` 注释分隔**：`累计 · {10} 条` 在 HTML 里是 `累计 · <!-- -->10<!-- --> 条`，grep 精确串会落空，属正常渲染。
- `next dev` 会**自动改写 `AGENTS.md`**（加回警告块），提交前 `git checkout -- AGENTS.md` 排除无关改动。
- 验证加载更多：绕过 M4 鉴权直接用 prisma 插 5 条测试数据 → dev 服务器看按钮/点击 → 完成后 `npx prisma db seed` 恢复种子（seed 对 Shiyu 是 `deleteMany` 重建，天然可重置）。

---

## M4 · 管理端（认证 + 后台）

### M4-1 NextAuth v4 + Next 16 集成
- **`middleware.ts` 已废弃 → `proxy.ts`**：Next 16 改名为 proxy，功能一致；`export { default } from "next-auth/middleware"` 这种 re-export 不被识别，要写 `export default withAuth({ pages: { signIn: "/login" } })`。
- **`prisma migrate dev` 在非交互 Bash 会拒绝**（"environment is non-interactive"）：改 schema 后手写迁移 SQL 到 `prisma/migrations/<时间戳>_<名>/migration.sql` → `npx prisma migrate deploy`（生产同款）；`npx prisma generate` 单独跑。⚠️ `cmd | tail` 会吞掉失败退出码，`&&` 仍会继续，注意区分。
- **登录支持「账号或邮箱」**：User 表加 `username String? @unique`，`authorize` 里 `findFirst({ where: { OR: [{ email }, { username }] } })`；登录表单字段名 `identifier`（type=text，非 email）。
- v4 稳定版已支持 Next 16 / React 19（peer 里明确 `^16` / `^19`）；v5 仍是 beta，选 v4。
- 结构：`lib/auth.ts` 导出 `authOptions`（Credentials + bcrypt + JWT session）；`app/api/auth/[...nextauth]/route.ts` 里 `const handler = NextAuth(authOptions); export { handler as GET, handler as POST }`。
- **类型扩展**：`types/next-auth.d.ts` 里 `declare module "next-auth"` 加 `Session.user.id/role`、`User.role`、`next-auth/jwt` 的 `JWT.id/role`——否则 session.user.role 没类型。
- `getServerSession(authOptions)` 在服务端组件 / Route Handler 都能用；写操作统一 `requireAdmin()`（M3 占位在 M4 替换为真实实现）。
- **`npm run start` 在 Windows 上停不掉子进程**：TaskStop 只杀 npm 包装，`next start` 子进程残留占端口。换端口/重启前要 `netstat -ano | findstr :3100` → `taskkill //F //PID`。

### M4-2/4-3 守卫与后台壳
- 双层防护：`proxy.ts` 未登录重定向 `/login`（体验）+ `(admin)/layout.tsx` 里 `getServerSession` + `role !== "ADMIN"` → `redirect("/login")`（权威校验）。
- 登录页客户端组件：`signIn("credentials", { email, password, redirect: false })` → 成功 `router.push("/dashboard")` + `router.refresh()`。
- 后台壳 `AdminShell`：深墨侧栏 `bg-ink text-paper`（`aside` sticky 满高）+ 纸面主区；移动端顶栏 + 横排导航。**只用 `bg-night` 反色带（同前台）**，不用 `bg-ink` 会随主题翻转。

### M4-4 嵌套 `<a>` 水合崩溃（React error #418）
- **`<a>` 内不能再嵌 `<a>`**：/posts 文章卡片整体是 `<Link>`，内部标签又用 `<Link>` → 非法 HTML → 水合失败（dev 报错 `In HTML, <a> cannot be a descendant of <a>`）。
- 修复：父级是链接时，内部 chip 一律用 `<span>`；要可点则重构卡片结构（标题单独 Link）。
- 后台文章表单：标签逗号分隔 → 服务端 `upsertTags`（github-slugger 生成 slug）→ `connect`；slug 重复捕 `P2002` → 409。

### M4-6 评论：访客按邮箱 find-or-create
- `Comment` 模型强制 `authorId`（外键到 User）。访客评论用 `user.upsert({ where: { email }, create: { email, name, role: "USER" } })`（无密码不可登录），生成邮箱兜底 `${Date.now()}@guest.local`。
- seed 里 **删表顺序要照顾外键**：先 `comment.deleteMany()` 再 `post.deleteMany()`，否则 FK 违约（post 有评论时删 post 报错）。

### M4-7 阅赞
- `like`/`view` 都是 `update({ data: { likes: { increment: 1 } } })`；防刷由前端做：view 用 `sessionStorage` 每篇每会话一次，like 前端 `disabled` 防连点。

---

## M5 · 打磨

### M5-1 搜索
- Prisma `contains` → PostgreSQL `ILIKE %q%`（大小写不敏感）。开发够用，生产大数据量再上 `tsvector`。
- `/search` 页用 `<form action="/search" method="get">`（GET，无 JS 依赖），`searchParams` 也是 Promise 需 `await`。

### M5-2 SEO/OG
- 根布局 `metadataBase: new URL(SITE_URL)` + `title.template` + `openGraph` 默认值。
- JSON-LD：RSC 里直接渲染 `<script type="application/ld+json">` 会进 HTML（SEO 生效）；dev 控制台有 "Encountered a script tag" warning 是**良性**（React 提示脚本不执行，对数据类 JSON-LD 正是想要的行为）。
- `app/sitemap.ts` + `app/robots.ts` 是约定文件，构建后 `GET /sitemap.xml` / `/robots.txt` 直接可用。

### M5-3 next/image
- `remotePatterns` 配 `images.unsplash.com`（种子图源）；封面/项目图用 `fill` + 父容器 `aspect-[16/9]` 或 `aspect-[16/10]` + `sizes`。
- **MDX 正文图不能转 next/image**：宽高未知，`fill` 需要容器尺寸。保留 `<img loading="lazy">` + 双规则 eslint-disable。
- 验证优化生效：HTML 里出现 `/ _next/image?url=...&w=640&q=75` + srcset。

### M5-4 暗黑模式回归
- 主题持久化在 localStorage；导航到新页后 `document.documentElement.classList` 保留 `.dark`（init 脚本读取）。回归=逐页 `navigate` + 看 console errors。
- 暗色下全页 0 error（除 dev 的 JSON-LD script warning）。

---
