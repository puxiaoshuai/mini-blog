# 任务清单 · 「大道至简」博客

> 配套《技术方案.md》使用。**M1 已完成**；从 M2 起每完成一步，勾选 ✅ 并把核心知识点记入《知识点.md》。
> 约定：`cd F:\mini-blog\blog-project` 为工作目录。

---

## M1 ✅ 基建（已完成 2026-08-11）

- [x] Next.js 16 脚手架（blog-project/，TS + Tailwind 4 + App Router，无 src）
- [x] 纸感编辑风设计系统落地（CSS 变量亮/暗双套、暗黑切换、噪点/纸框/chip/u-link/card-raise/动效）
- [x] 字体改为 npm `@fontsource` 自托管（国内网络不可达 Google Fonts）
- [x] Prisma 7 + PostgreSQL（Docker 本地）：schema（Post/Tag/User/Comment/Shiyu）+ 迁移 + 种子（管理员 + 10 拾语 + 2 文章 + 2 标签）
- [x] 根布局 + `(blog)` 布局（TopBar / Header / Footer）+ 8 条静态路由
- [x] 首页（刊头 / 卷首诗 / 数据台账 / 最近文章 / 项目 / 订阅带）+ 导航含「项目」

---

## M2 ✅ 内容模块（MDX + 文章链路线，2026-08-11）

- [x] M2-1 接入 `next-mdx-remote` v6 + `lib/mdx.ts`（DB 为数据源，MDX 存 `Post.content`）
- [x] M2-2 种子文章 MDX 内容（含标题 / 表格 / 代码 / 引文）+ 封面
- [x] M2-3 文章列表页 `/posts`：SSG 查 DB 已发布文章（编号列表）
- [x] M2-4 文章详情页 `/posts/[slug]`：SSG + `generateStaticParams`，MDX 渲染 + 目录侧栏
- [x] M2-5 `MDXComponents`：首字下沉 / 引文 / 代码块 / 表格等自定义组件
- [x] M2-6 标签/归档页 `/tags` + `/tags/[tag]`：左栏分类索引 + 按月归档时间线
- [x] M2-7 首页接数据库：数据台账真实计数、最近文章真实列表

## M3 ✅ 拾语模块（动态流，2026-08-11）

- [x] M3-1 `lib/shiyu.ts` 查询封装（置顶优先 + 时间倒序 + 分页；Shiyu 增 `no` 序号字段，种子 N°027–036）
- [x] M3-2 `/api/shiyu` 与 `/api/shiyu/[id]`（GET/POST/PUT/DELETE，写操作 `requireAdmin()` 守卫，M4 接 NextAuth）
- [x] M3-3 `ShiyuTimeline` 组件（mono 编号 N°、日期、一句话、`next/image` 配图懒加载、置顶 chip）+ `ShiyuStream` 客户端加载更多
- [x] M3-4 `/shiyu` 页（ISR `revalidate=60`）实现，对照 `design/shiyu.html` 验收通过

## M4 ✅ 管理端（认证 + 后台，2026-08-11）

- [x] M4-1 NextAuth.js v4 接入（凭证登录、bcrypt 校验、JWT session 注入 id/role；`proxy.ts` 守卫）
- [x] M4-2 登录页 `/login` + 权限守卫（`(admin)` 路由组 getServerSession 校验 ADMIN）
- [x] M4-3 `(admin)/dashboard` 布局（深墨侧栏 + 纸面主区，含移动端）
- [x] M4-4 文章管理 CRUD（列表 / 新建 / 编辑 / 删除，MDX 内容 + 标签自动 upsert）
- [x] M4-5 拾语管理（后台「拾语」分栏：新建 / 发布切换 / 置顶切换 / 删除）
- [x] M4-6 评论管理（待审列表 / 审核通过 / 删除；文章页公开评论 + 访客提交待审）
- [x] M4-7 阅赞计数 API（`/api/posts/[id]/like` + `/view`，文章页交互组件）

## M5 ✅ 打磨（性能 / SEO，2026-08-11）

- [x] M5-1 全文搜索（开发 LIKE；生产可切 PostgreSQL `tsvector`）+ `/search` 页 + 搜索入口
- [x] M5-2 SEO/OG：根布局 metadataBase/OG、文章页 OG + JSON-LD（BlogPosting）、sitemap、robots
- [x] M5-3 性能 + `next/image` 图片优化（封面/项目图/拾语配图全走优化器）
- [x] M5-4 暗黑模式全页回归（顺带修复 /posts 嵌套 `<a>` 水合 bug）
- [x] ~~M5-5 真实内容迁移~~ **已取消**（用户决定不做真实内容迁移，种子数据即演示数据）

## M6 · 上线

- [ ] M6-1 Vercel 部署（root dir = blog-project，环境变量配置）
- [ ] M6-2 域名绑定 `puxiaoshuai.top` + 备案
- [ ] M6-3 真实数据导入 + 关于 / 项目真实信息（等用户提供）
- [ ] M6-4 上线验收

---

## 待用户提供（阻塞部分步骤）

1. 真实项目信息（名 / 简介 / 演示链接 / GitHub）→ 填 `blog-project/lib/projects.ts`（静态数据文件，非数据库）→ M6-3
2. 真实文章封面图 → M6-3
3. ~~WordPress 导出数据（文章 / 拾语 / 评论 / 标签）→ M5-5~~（M5-5 已取消，不再需要）
