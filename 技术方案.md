# 技术方案 · 「大道至简」个人博客

> 站点定位：个人技术博客 + 生活记录，从 WordPress 迁移到 Next.js 全栈。
> 站点信息：站名「大道至简」，副题「时光是画在卷上的河流」，作者 蒲小帅（puxiaoshuai）。
> 设计基准：`design/` 下的高保真设计稿（index / post / tag / about / shiyu / admin）+ `design/design.md` 设计系统。
> 文档依据：`readme.md`（目录结构 / 数据表 / 拾语模块 / 架构图）。

---

## 1. 项目概述

### 1.1 背景

从 WordPress（`puxiaoshuai.top`，Qzdy 主题）迁移到现代技术栈，重构为 Next.js 全栈应用，落地「纸感编辑风」设计系统。

### 1.2 目标

| 目标 | 说明 |
|---|---|
| 内容展示 | 文章（MDX）SSG 预渲染，秒开、利于 SEO |
| 动态更新 | 拾语流（一句话 / 一句话+配图）由数据库驱动 |
| 后台管理 | 管理员维护文章、拾语、评论，普通用户仅浏览 |
| 全栈能力 | 文章增删改查、全文搜索、阅读量/点赞、评论 |
| 品牌设计 | 延续暖纸 / 墨黑 / 朱红「纸感编辑风」，含暗黑模式 |

### 1.3 现状（设计稿已完成）

- `design/` 六页高保真 HTML 稿已用真实文章内容填充，暗黑模式已支持。
- 拾语页含 10 条种子数据（N°027–036）与设计稿一致。
- **待办**：真实项目信息（名/简介/演示/GitHub 链接）、文章封面图、WordPress 导出数据。

---

## 2. 技术选型

| 领域 | 选型 | 版本/说明 | 理由 |
|---|---|---|---|
| 框架 | Next.js（App Router） | 16.3（Turbopack） | 服务端渲染 + 文件路由 + RSC，SSG/ISR/API 一体 |
| 语言 | TypeScript | 5.x | 类型安全，配合 Prisma 生成类型 |
| 样式 | Tailwind CSS | 4.x | 与设计系统 CSS 变量无缝映射，支持暗黑模式 |
| 内容 | MDX + next-mdx-remote | 最新稳定版 | 文章以 MDX 文件形式存放，渲染时编译为 React 组件（选型已确认） |
| 数据库 | SQLite（开发）/ PostgreSQL（生产） | driver adapter | 开发零配置；生产切 Turso / Neon |
| ORM | Prisma | 7.x（prisma.config.ts + driver adapter） | schema 即文档，迁移 + seed 一体 |
| 字体 | @fontsource（npm 自托管） | 5.x | 国内网络无需访问 Google Fonts，随 npm 安装下载 |
| 缓存 | Redis（Upstash） | — | 热点数据缓存、限流（可选） |
| 认证 | NextAuth.js（Auth.js） | v4/v5 | 凭证登录 + 后续可扩展 OAuth |
| UI 组件 | shadcn/ui | — | 少量基础组件（表单、弹窗），覆盖设计 token |
| 部署 | Vercel | Edge Network | 与 Next.js 集成最佳，ISR/图片优化开箱即用 |

> ✅ **内容层选型已确认（2026-08-11）**：`readme.md` 中 `lib/contentlayer.ts` 指向的 Contentlayer 已停止维护，改用 **next-mdx-remote**（MDX 渲染时编译为 React 组件）。目录中 `lib/contentlayer.ts` 落地时替换为 MDX 读取/编译工具。

---

## 3. 系统架构

```
┌─────────────────────────────────────────────────────┐
│                   用户浏览器                        │
│         (HTTP/HTTPS + 静态资源请求)                │
└─────────────────┬───────────────────────────────────┘
                  │
┌─────────────────▼───────────────────────────────────┐
│              Vercel (Edge Network)                  │
│  ┌────────────────────────────────────────────┐    │
│  │        Next.js 15 App Router              │    │
│  ├────────────────────────────────────────────┤    │
│  │  SSG Pages  │  ISR Pages  │ API Routes   │    │
│  │  (静态生成) │ (增量重验证)│ (RESTful)    │    │
│  └────────────────────────────────────────────┘    │
└─────────────────┬───────────────────────────────────┘
                  │
    ┌─────────────┼─────────────┐
    │             │             │
┌───▼────┐  ┌────▼────┐  ┌───▼────┐
│ Content │  │ Prisma  │  │ Redis  │
│ layer   │  │  ORM    │  │ Cache  │
│ (MDX)   │  └────┬────┘  │(Upstash)│
└─────────┘       │       └─────────┘
           ┌──────▼──────┐
           │  PostgreSQL │
           │  (Turso/    │
           │   Neon)     │
           └─────────────┘
```

### 3.1 数据流

- **文章 / 拾语**：内容写入 DB → 写入/变更时触发 `revalidatePath`/`revalidateTag` 刷新对应 ISR 页面缓存 → 用户读取到最新数据。
- **前台展示**：静态内容（文章列表、标签归档）走 SSG；动态内容（阅赞数、拾语流）走 ISR + 按需刷新。
- **管理端**：登录态由 NextAuth session 校验 → 调 API Routes → Prisma 读写 → 触发缓存刷新。

### 3.2 渲染策略

| 内容 | 策略 | 说明 |
|---|---|---|
| 首页 / 标签页 / 文章详情 | SSG（`generateStaticParams`） | 构建时生成静态页，SEO 友好 |
| 拾语页 `/shiyu` | ISR（`revalidate`） | 内容高频更新，增量重验证 |
| 管理端 `/dashboard` | 客户端 + RSC 混合 | 需登录态，动态渲染 |
| 评论、阅赞 | CSR（API 调用） | 交互式数据 |
| 全文搜索 | CSR + API | 服务端 Prisma 全文检索 |

---

## 4. 目录结构规划

对齐 `readme.md`，补充设计稿对应的页面路由：

```
blog-project/
├── app/
│   ├── (blog)/                      # 前台路由组（共享站点布局）
│   │   ├── posts/
│   │   │   ├── [slug]/page.tsx      # 文章详情页（SSG）
│   │   │   └── page.tsx             # 文章列表页
│   │   ├── tags/[tag]/page.tsx      # 标签筛选页
│   │   ├── shiyu/page.tsx           # 拾语页（动态流：一句话 / 一句话+配图）
│   │   ├── about/page.tsx           # 关于我页（设计稿已有，readme 目录待补）
│   │   └── layout.tsx               # Header / Footer / 顶部通告条
│   ├── (admin)/
│   │   ├── dashboard/page.tsx       # 后台管理（权限保护，含「拾语」分栏）
│   │   └── layout.tsx               # 深墨侧栏布局
│   ├── api/
│   │   ├── posts/
│   │   │   ├── route.ts             # GET/POST 文章
│   │   │   └── [id]/route.ts        # PUT/DELETE 文章
│   │   ├── shiyu/
│   │   │   ├── route.ts             # GET/POST 拾语
│   │   │   └── [id]/route.ts        # PUT/DELETE 拾语
│   │   ├── auth/[...nextauth]/route.ts  # NextAuth 处理
│   │   └── search/route.ts          # （建议）全文搜索接口
│   └── layout.tsx                   # 根布局（字体 / 主题脚本 / 噪点）
├── components/
│   ├── ui/                          # shadcn/ui 组件
│   ├── posts/
│   │   ├── PostCard.tsx             # 首页文章卡（编号 + 宋体标题 + 标签）
│   │   ├── PostList.tsx             # 列表/归档时间线
│   │   └── MDXComponents.tsx        # 自定义 MDX 组件（首字下沉/引文/代码块…）
│   ├── shiyu/
│   │   └── ShiyuTimeline.tsx        # 拾语时间线（置顶 + 配图 + mono 编号）
│   └── common/
│       ├── Header.tsx               # 页头（品牌 + 导航 + 主题切换 + 管理入口）
│       ├── Footer.tsx               # 页脚
│       └── Toc.tsx                  # 文章目录侧栏（建议）
├── content/
│   ├── posts/*.mdx                  # 所有 MDX 文章
│   └── authors/                     # 作者信息
├── lib/
│   ├── db.ts                        # Prisma 客户端（单例）
│   ├── contentlayer.ts              # 内容层配置（见 2. 选型提示）
│   └── utils.ts                     # cn()、日期格式化等
├── prisma/
│   ├── schema.prisma
│   └── seed.ts                      # 种子数据（含 10 条拾语 N°027–036）
├── types/index.ts                   # 共享类型
└── next.config.js
```

---

## 5. 数据模型设计

> 依据 `readme.md` 数据表。实体关系：

```
User 1 ── n Post        （authorId）
User 1 ── n Comment     （authorId）
User 1 ── n Shiyu       （authorId）
Post n ── n Tag         （隐式多对多）
Post 1 ── n Comment     （postId）
```

### 5.1 模型要点

**Post（文章）**
- `slug` 唯一，用于路由；`content` 存 MDX 文本（`@db.Text`）。
- `views` / `likes` 为计数器，点赞可后续升级为「按用户去重」。
- 索引 `[published, createdAt]` 覆盖「已发布 + 时间排序」主查询。
- 全文检索：SQLite（Prisma 7 + better-sqlite3 不支持 `@@fulltext`）暂用 LIKE 检索；生产切 PostgreSQL 后改用 `tsvector` 全文索引。

**Tag（标签）**
- `name` / `slug` 双唯一；`Post[]` 隐式多对多关系表由 Prisma 自动维护。
- 归档页按「栏目 + 标签」双组展示（见设计稿 tag.html）。

**User（用户）**
- `password?` 仅在凭证登录时使用（bcrypt 哈希）。
- `role: Role`（`USER` / `ADMIN`），后台接口统一做角色校验。

**Comment（评论）**
- `published` 默认 true，可做审核开关；与 Post / User 均有关联。
- 列表查询按 `postId + createdAt` 排序。

**Shiyu（拾语）** ⭐ 特色模块
- `content`：一句话正文；`images`：配图 JSON 数组字符串（可空），如 `["/uploads/a.jpg"]`。
- `pinned`：置顶，列表按「置顶优先 + 时间倒序」排序。
- `published`：发布状态，管理端「拾语」分栏维护，前台只读。
- 索引 `[published, createdAt]`。
- 种子数据 10 条：编号 N°027–036，5 条纯文字 + 5 条配图，与 `design/shiyu.html` 一致。


---

## 6. 页面功能设计（设计稿 → 实现映射）

### 6.1 首页 `/`
- **区块**：刊头双线 → 卷首诗 hero → 数据台账（四格数字）→ 文章卡片网格 → 项目 → 标签云 → 订阅带 → 页脚。
- **实现**：SSG；数据台账/订阅区静态；文章网格查 DB；「项目」板块待真实数据（当前为占位墨笺/诗笺）。

### 6.2 文章详情 `/posts/[slug]`
- **区块**：面包屑 → 头部（chip 标签 + 宋体大标题 + 作者/日期/阅赞）→ 封面 → 正文 → 标签+分享 → 作者卡 → 上下篇 → 评论区；桌面右侧 sticky 目录侧栏。
- **实现**：SSG + `generateStaticParams`；正文由 MDX 渲染，`MDXComponents` 覆盖首字下沉/引文/代码块/表格；阅赞走 API 客户端更新。

### 6.3 标签 / 归档 `/tags/[tag]`
- 左栏 sticky 分类索引（栏目 + 标签双组，active 左缘朱红条）+ 右栏「# 标签 hero」+ **按月归档时间线**（竖线 + 月份节点圆点）。
- **实现**：SSG，按标签分组聚合文章并按月归档。

### 6.4 拾语 `/shiyu`
- 一句话 或 一句话+配图的时间线，置顶置前，mono 编号（N° 0xx）+ 日期。
- **实现**：ISR；`ShiyuTimeline` 组件；配图懒加载。

### 6.5 关于我 `/about`
- 设计稿已有（about.html），readme 目录未列，**落地时补 `/about` 路由**。

### 6.6 管理端 `/dashboard`
- 深墨侧栏（`bg-ink text-paper`）+ 纸面主区；统计卡 + SVG 面积图 + 文章表格（状态 pill）+ 待审评论 + **拾语分栏**。
- **实现**：`(admin)` 路由组，`middleware.ts` 或布局层校验 `session.user.role === ADMIN`。

---

## 7. API 设计（RESTful）

### 7.1 文章 `/api/posts`
| Method | Path | 功能 | 权限 |
|---|---|---|---|
| GET | `/api/posts` | 文章列表（分页/按发布状态筛选） | 公开 |
| GET | `/api/posts/[id]` | 文章详情（阅数 +1） | 公开 |
| POST | `/api/posts` | 新建文章 | ADMIN |
| PUT | `/api/posts/[id]` | 更新文章 | ADMIN |
| DELETE | `/api/posts/[id]` | 删除文章 | ADMIN |

### 7.2 拾语 `/api/shiyu`
| Method | Path | 功能 | 权限 |
|---|---|---|---|
| GET | `/api/shiyu` | 拾语流（置顶优先 + 时间倒序，支持分页） | 公开 |
| POST | `/api/shiyu` | 新建拾语 | ADMIN |
| PUT | `/api/shiyu/[id]` | 更新拾语 | ADMIN |
| DELETE | `/api/shiyu/[id]` | 删除拾语 | ADMIN |

### 7.3 认证 `/api/auth/[...nextauth]`
- 凭证登录（email + password，bcrypt 校验）。
- Session 含 `role`，供前后台权限判断。
- 环境变量：`DATABASE_URL`、`NEXTAUTH_SECRET`、`NEXTAUTH_URL`。

### 7.4 其他（建议）
- `/api/search?q=` 全文搜索（Prisma 全文索引，返回文章标题/摘要命中）。
- `/api/posts/[id]/like` 点赞、`/api/posts/[id]/view` 阅读数（防刷：按 IP/Session 限频）。

> 写入操作后统一触发 `revalidatePath` / `revalidateTag`，保证 ISR 页面与 API 一致。

---

## 8. 认证与权限

1. **NextAuth.js 配置**（`app/api/auth/[...nextauth]/route.ts`）：
   - Provider：Credentials（凭证登录）。
   - `authorize` 回调用 Prisma 查 `User` + bcrypt 比对密码。
   - `session` 回调注入 `user.id` / `user.role`。
2. **前台**：Header 右侧提供「登录/管理」入口；未登录仅可浏览。
3. **后台**：`(admin)` 路由组 + 服务端 `getServerSession` 校验 `role === 'ADMIN'`，不满足则重定向 `/api/auth/signin`。
4. **API 保护**：写操作（POST/PUT/DELETE）统一在路由内校验 session + role，不可仅靠前端隐藏入口。

---

## 9. 设计系统落地

按 `design/design.md` 的「复用清单」落地：

| 项 | 落地方式 |
|---|---|
| 颜色 token | `:root` / `html.dark` 两套 CSS 变量（`rgb(r g b)`），Tailwind `theme.extend.colors` 映射为 `paper/ink/accent/...` |
| 暗黑模式 | `html.dark` 切换 + `<head>` 内联防闪白脚本 + 主题按钮（localStorage + 跟随系统） |
| 字体 | 根布局引入 Google Fonts：Noto Serif SC / Noto Sans SC / IBM Plex Mono |
| 纸面颗粒 | `body::before` 固定层噪点 SVG |
| 纸框投影 | `.frame { box-shadow: 6px 6px 0 0 … }` |
| 小组件 | 全局 `chip` / `u-link`（下划线动画）/ `eyebrow` / `reveal`（入场） |
| 动效 | 刊头墨水扫入、卡片悬浮、数字滚动（IntersectionObserver）、印章呼吸 |
| 页面骨架 | `max-w-6xl mx-auto px-5`；sticky 页头；`bg-night text-nightText` 深色带（反色带不用 `bg-ink`） |

---

## 10. 性能与 SEO

- **性能**：SSG 静态页 + 图片懒加载/`next/image`；首页首屏零客户端 JS（RSC）。
- **SEO**：`generateMetadata` 生成 title/description/OG；文章页注入结构化数据（JSON-LD）。
- **全文搜索**：Prisma 全文索引（生产 PostgreSQL 可用 `tsvector`）。
- **缓存**：ISR 按需重验证；热点数据可选 Redis 缓存；`next/image` 自动优化。

---

## 11. 部署与运维

| 项 | 方案 |
|---|---|
| 托管 | Vercel（`vercel.json` 配置 build / ISR 友好） |
| 数据库 | 开发：SQLite 文件；生产：Turso / Neon（PostgreSQL），`DATABASE_URL` 切换 |
| 迁移 | `prisma migrate deploy`（CI 或构建前执行） |
| 种子 | `prisma db seed`（`seed.ts` 含 10 条拾语） |
| 环境变量 | `DATABASE_URL` / `NEXTAUTH_SECRET` / `NEXTAUTH_URL`（Vercel Project Settings） |
| 域名 | 绑定 `puxiaoshuai.top`，DNS 指向 Vercel；备案沿用现有蜀ICP |
| 图片 | 封面/拾语配图建议用 Vercel Blob 或现有图床，`next/image` 可接受外部 URL |

---

## 12. 开发里程碑

| 阶段 | 内容 | 产出 |
|---|---|---|
| M1 基建 | Next.js 初始化、Tailwind + 设计 token、Prisma schema + 迁移 + seed、布局（Header/Footer/通告条） | 空壳可跑，主题可切换（✅ 进行中） |
| M2 内容 | MDX 内容层接入、文章列表/详情 SSG、MDX 组件、标签/归档页 | 前台文章链路线打通 |
| M3 拾语 | `Shiyu` API + `/shiyu` ISR 页 + 时间线组件 | 拾语模块上线 |
| M4 管理端 | NextAuth 登录、`(admin)` 路由组、文章/拾语/评论 CRUD、阅赞评论 | 后台可维护 |
| M5 打磨 | 全文搜索、SEO/OG、性能审计、暗黑模式回归、真实内容迁移 | 上线 |
| M6 上线 | Vercel 部署、域名绑定、真实数据导入、关于/项目真实信息 | 正式运行 |

---

## 附：待用户提供 / 遗留项

1. 真实项目信息（名 / 简介 / 演示链接 / GitHub 链接）→ 首页「项目」板块、关于页。
2. 真实文章封面图。
3. WordPress 导出数据（文章 / 拾语 / 评论 / 标签）。
4. ~~内容层选型确认~~（✅ 已定：next-mdx-remote，2026-08-11）。
5. `readme.md` 目录未列 `/about` 与 `/api/search`，落地时需补充。
