# 本地开发数据库：PostgreSQL
# 启动：docker compose up -d    停止：docker compose down（数据在卷里保留）
services:
  db:
    image: postgres:16-alpine
    container_name: mini-blog-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: mini_blog
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d mini_blog"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
